# Extracted Home Page Assets

This folder contains assets extracted from the root `index.html` SingleFile capture.

- `styles.css`: extracted inline CSS.
- `media/`: images that were previously embedded as `data:image` URLs.
- `fonts/`: fonts that were previously embedded as base64 `@font-face` sources.
